version: 2
 
models:
  - name: non_daily_orders_results
    description: "Expectation test for null value"
    columns:
      - name: department_id
          test:
          - dbt_expectations.expect_column_values_to_be_between
              min_value: 0
              max_value: 25

    columns:
      - name: aisle_id
          test:
          - dbt_expectations.expect_column_values_to_be_between
              min_value: 0
              max_value: 135